import React from 'react';
import styled from "@emotion/styled";

export const Board = () => {
    return (
        <Container>
        </Container>
    );
}

const Container = styled.div`
  flex: 1;
`;
