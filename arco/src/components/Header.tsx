import React from 'react';
import styled from "@emotion/styled";

export const Header = () => {
    return (
        <Container>
        </Container>
    );
}

const Container = styled.div`
  height: 50px;
`
